import styled from 'styled-components';

export const LateralBar = styled.div`
  background-color: var(--side-bar-color);
  height: 100%;
  width: min(5rem, 20%);

  border-right: 0.1rem solid var(--wrapper-border-color);
`;